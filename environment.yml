name: db
channels:
  - defaults
dependencies:
  - python=3.9
  - pip:
      - accelerate==1.2.0
      - numpy==1.22.4
      - pandas==2.2.3
      - torch==2.5.1
      - transformers==4.47.0
prefix: /opt/miniconda3/envs/db
